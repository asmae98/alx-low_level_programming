#include <stdio.h>
#include <string.h>

char* leet(char* str);

int main() {
    char str[] = "leet is elite";
    
    printf("Before: %s\n", str);
    
    leet(str);
    
    printf("After: %s\n", str);
    
    return 0;
}





